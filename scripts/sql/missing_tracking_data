-- Aggregrates null tracking data from tracking table.

SELECT t1.pitchno
FROM dbo.pitches t1
LEFT JOIN dbo.tracking t1
    ON t1.game_pk = t2.game_pk
    AND t1.pitcher_id = t2.pitcherid
    AND t1.batter_id = t2.batterid
    AND t1.pitch_number = t2.pitchno
WHERE t1.pitchno IS NULL
